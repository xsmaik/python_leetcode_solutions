class Solution:
    def isBalanced(self, num: str) -> bool:
        left = 0
        right = 0
        num = [int(i) for i in num]

        left += sum(num[::2])
        right += sum(num[1::2])

        return left == right
