class Solution:
    def minOperations(self, nums: List[int], k: int) -> int:
        total = 0

        for i in nums:
            if i < k:
                total += 1
                
        return total
