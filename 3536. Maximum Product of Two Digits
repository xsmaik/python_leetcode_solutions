class Solution:
    def maxProduct(self, n: int) -> int:
        arr = [int(i) for i in str(n)]
        res = []

        for i in range(len(arr)):
            for j in range(i):
                res.append(arr[i] * arr[j])
                
        return max(res)
