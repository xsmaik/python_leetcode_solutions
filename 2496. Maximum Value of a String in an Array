class Solution:
    def maximumValue(self, strs: List[str]) -> int:
        res = 0

        for word in strs:
            if word.isdigit():
                res = max(res, int(word))
            else:
                res = max(res, len(word))

        return res
