class Solution:
    def hasSameDigits(self, s: str) -> bool:
        s = [int(i) for i in s]
        while len(s) > 2:
            s = [(s[i] + s[i + 1]) % 10 for i in range(len(s) - 1)]

        if s[0] != s[1]:
            return False
        else:
            return True
