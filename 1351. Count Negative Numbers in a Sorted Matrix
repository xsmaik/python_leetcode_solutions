class Solution:
    def countNegatives(self, grid: List[List[int]]) -> int:
        total = 0

        for row in grid:
            for i in row:
                if i < 0:
                    total += 1

        return total
