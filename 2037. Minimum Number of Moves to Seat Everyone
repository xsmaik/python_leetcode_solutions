class Solution:
    def minMovesToSeat(self, seats: List[int], students: List[int]) -> int:
        seats.sort()
        students.sort()
        res = []

        for i in range(len(seats)):
            res.append(abs(students[i] - seats[i]))

        return sum(res)
