class Solution:
    def numJewelsInStones(self, jewels: str, stones: str) -> int:
        set_jewels = set(jewels)
        count = 0

        for i in stones:
            if i in set_jewels:
                count += 1
        return count 
