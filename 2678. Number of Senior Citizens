class Solution:
    def countSeniors(self, details: List[str]) -> int:
        total = 0

        for i in details:
            age = int(i[-4:-2])
            if age > 60:
                total += 1

        return total
