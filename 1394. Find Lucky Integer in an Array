class Solution:
    def findLucky(self, arr: List[int]) -> int:
        res = []

        for i in range(len(arr)):
            if arr[i] == arr.count(arr[i]):
                res.append(arr[i])

        if res:
            return max(res)
        else:
            return -1

        
