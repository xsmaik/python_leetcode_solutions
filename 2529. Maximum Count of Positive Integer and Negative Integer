class Solution:
    def maximumCount(self, nums: List[int]) -> int:
        plus = 0
        minus = 0

        for i in nums:
            if i == 0:
                continue
            elif i > 0:
                plus += 1
            else:
                minus += 1

        return max(plus, minus)
